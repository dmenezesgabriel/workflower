version: "1.0"
workflow:
  name: cli_tableau_linter
  jobs:
    - name: tableau_linter_module
      operator: module
      module_path: "samples/modules/tableau_linter/tableau_linter.py"
      module_name: "tableau_linter"
      plugins:
        - tableau_document_plugin
      trigger: date
